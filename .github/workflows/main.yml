name: Ejemplo de Action

on:
  push:
 

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Imprimir mensaje
      run: echo "¡Hola, este es un ejemplo de GitHub Actions que se ejecuta cada que hago push"
 
